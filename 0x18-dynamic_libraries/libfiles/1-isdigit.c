#include "main.h"

/**
 * _isdigit - checks for digits.
 *@c: c is one character
 *
 * Return: 1 if c is digit else returns 0
 */
int _isdigit(int c)

{
	if (c >= '0' && c <= '9')
	return (1);
	else
	return (0);


}
