#include "main.h"

/**
 * print_numbers - prints the first ten digits
 *
 * Return : returns void
 */

void print_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		_putchar(i + 48);
}
	_putchar(10);
}
