#include "main.h"

/**
 * _strlen - counts the lenght of a string
 * @s : string
 *
 * Return: i the lenght of a string
 */

int _strlen(char *s)
{

	int i;

	for (i = 0; s[i] != '\0'; i++)
{
	i = i;
}
	return (i);
}
